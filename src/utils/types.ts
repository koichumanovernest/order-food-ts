export type MealItem = {
  title: string;
  description: string;
  id: number;
  price: string;
};

export type BasketItemType = {
  title: string;
  amount: number;
  id: number;
  price: string;
};
